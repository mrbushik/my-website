export interface aboutMeItemsInterface {
  title: string;
  url: string;
  background: string;
  link: string;
  icon: any;
}
export interface experienceInterface {
  year: number | string;
  length?: string;
  company: string;
  description: string;
}
export interface contacts {
  imgLink: string;
  itemLink: string;
  title: string;
}
